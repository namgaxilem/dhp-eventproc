import { Form } from 'antd';
import Props from './Zookeeper.props';

const Zookeeper = ({}: Props) => {
    return <>
        <Form
            name="basic"
            labelCol={{ span: 14 }}
            wrapperCol={{ span: 10 }}
            autoComplete="off">

        </Form>
    </>
}

export default Zookeeper;